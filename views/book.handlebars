{{!-- This handlebars is for displaying the actual book (single book) --}}

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <h2>Baby Books</h2>

            <ul>
                {{#each Book}}
                    {{title}}
                {{/each}}
            </ul> 
        </div>
    </div>
</div>